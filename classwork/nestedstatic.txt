package innerclasspack;class Example{	static int count;}public class NestedStaticExample {	public static void main(String[] a)	{		System.out.println(Example.count);				Outer.Inner inner=new    Outer.Inner();		inner.displayinner();				Outer outer=new Outer();				outer.displayOuter();	}}class Outer{	String outerMessage="outer";	 static class Inner	{		public Inner()		{					}		String innerMessage="inner";		void displayinner()		{			System.out.println("Hello inner class"+new Outer().outerMessage+ " "+innerMessage );		}	}		void displayOuter()	{		System.out.println("Hello OUTER class"+outerMessage+ " "+new Inner().innerMessage );	}}



A static class is a class that is created inside a class, is called a static nested class in Java. It cannot access non-static data members and methods. It can be accessed by outer class name.It can access static data members of the outer class, including private.The static nested class cannot access non-static (instance) data members or
